# 计算机组成原理：C++ 转 8086 汇编终极冲刺必背

## 🌟 考前默写“三大黄金法则”

1. **函数起手收尾式**：开头必写 `PUSH BP` / `MOV BP, SP`，结尾必写 `POP BP` / `RET`。
    
2. **条件反向跳跃**：汇编没有大括号，遇到 `if (i < 5)`，写成“比较 `i` 和 5，如果 `>= 5` (`JGE`)，就跳走”。
    
3. **数组索引必乘2**：在 16 位系统中，`int` 占 2 字节。读取 `array[i]` 时，地址偏移量必须是 `i * 2`（汇编中用 `ADD SI, SI` 实现）。
    

---

## 题型一：递归函数调用 (Recursive Function)

**【核心考点】**：栈帧建立、参数获取 `[BP+4]`、堆栈平衡 `ADD SP, 2`。

### 题目代码 (C++)

C++

```
int factorial(int n)
{
    if(n <= 1)
    {
        return 1;
    }
    return n * factorial(n - 1);
}
```

### 标准答案 (8086 Assembly)

代码段

```
FACTORIAL PROC
    ; 1. 函数起手式：建立栈帧
    PUSH BP
    MOV BP, SP
    
    ; 2. if (n <= 1)
    MOV AX, [BP+4]      ; 从堆栈获取参数 n
    CMP AX, 1           ; 比较 n 和 1
    JLE BASE_CASE       ; 如果 n <= 1，跳到递归出口
    
    ; 3. 准备调用 factorial(n - 1)
    DEC AX              ; 算出 n - 1
    PUSH AX             ; 将 n - 1 压入堆栈作为新参数
    CALL FACTORIAL      ; 递归调用
    
    ; 4. 堆栈平衡与乘法 (return n * ...)
    ADD SP, 2           ; 【极度重要】清理刚才压入的参数！
    MOV CX, [BP+4]      ; 重新把原来的参数 n 拿出来放到 CX
    MUL CX              ; AX = AX * CX (即 factorial(n-1) * n)
    JMP END_FUNC        ; 跳到函数结尾，避开 BASE_CASE
    
BASE_CASE:              ; 5. 递归出口 (return 1)
    MOV AX, 1           ; 返回值统一放在 AX 中
    
END_FUNC:
    ; 6. 函数收尾式：恢复现场
    POP BP
    RET
FACTORIAL ENDP
```

---

## 题型二：数组遍历与 if-else 分支

**【核心考点】**：参数传递（压入数组首地址）、`if-else` 的反向跳转逻辑、求相反数 `NEG`。

### 题目代码 (C++)

C++

```
void main() {
    int array[10];
    int n = 10;
    f(array, n);
}

void f(int [] a, int n) {
    for (int i = 0; i<n; i++) {
        if (i<5)
            a[i] = i;
        else
            a[i] = -i;
    }
}
```

### 标准答案 (8086 Assembly)

代码段

```
.DATA
    ARRAY DW 10 DUP(?)  ; 定义数组 array[10]
    N     DW 10         ; 定义变量 n = 10

.CODE
MAIN PROC
    ; --- 主程序调用逻辑 ---
    MOV AX, @DATA
    MOV DS, AX
    
    PUSH N              ; 压入第二个参数 n
    MOV AX, OFFSET ARRAY 
    PUSH AX             ; 压入第一个参数 (数组首地址)
    CALL F_FUNC         ; 调用 f(array, n)
    ADD SP, 4           ; 【关键】清理堆栈中的两个参数 (共4字节)
    
    MOV AH, 4CH         ; 退出程序
    INT 21H
MAIN ENDP

F_FUNC PROC
    ; --- 子函数 f 逻辑 ---
    PUSH BP
    MOV BP, SP
    
    MOV CX, 0           ; int i = 0 (用 CX 当作 i)

FOR_LOOP:
    CMP CX, [BP+6]      ; 比较 i 和参数 n (在 BP+6)
    JGE FOR_END         ; if (i >= n)，跳出循环

    CMP CX, 5           ; 比较 i 和 5
    JGE ELSE_PART       ; if (i >= 5)，跳到 else 分支

    ; --- if (i < 5) 的代码块 ---
    MOV BX, [BP+4]      ; 获取数组首地址
    MOV SI, CX          ; 准备计算偏移量
    ADD SI, SI          ; SI = i * 2 (因为 int 是 16 位)
    MOV [BX+SI], CX     ; a[i] = i
    JMP NEXT_ITER       ; 【关键】执行完 if，必须跳过 else！

ELSE_PART:
    ; --- else 的代码块 ---
    MOV BX, [BP+4]      ; 获取数组首地址
    MOV SI, CX
    ADD SI, SI          ; SI = i * 2
    MOV AX, CX          ; 把 i 移到 AX 中准备变负数
    NEG AX              ; AX = -i (求相反数指令)
    MOV [BX+SI], AX     ; a[i] = -i

NEXT_ITER:
    INC CX              ; i++
    JMP FOR_LOOP        ; 跳回循环头部进行下一次判断

FOR_END:
    POP BP
    RET
F_FUNC ENDP
```

---

## 题型三：全局变量数组求和

**【核心考点】**：数据段（`.DATA`）的全局变量定义、寄存器累加。

### 题目代码 (C++)

C++

```
int array[5];
int n = 5;
int sum = 0;
for (int i = 0; i<n; i++) {
    sum += array[i];
}
```

### 标准答案 (8086 Assembly)

_(注：这题是一段主逻辑，没有封装成函数，直接写主体代码)_

代码段

```
.DATA
    ARRAY DW 5 DUP(?)   ; int array[5];
    N     DW 5          ; int n = 5;
    SUM   DW 0          ; int sum = 0;

.CODE
    ; (假设前面已经有了 MOV AX, @DATA 和 MOV DS, AX)
    
    MOV CX, 0           ; int i = 0 (用 CX 当作循环变量 i)
    MOV AX, 0           ; 用 AX 临时存放累加和 (对应 sum)
    MOV BX, OFFSET ARRAY; BX 存放数组的首地址

SUM_LOOP:
    CMP CX, N           ; 比较 i 和 n
    JGE SUM_END         ; if (i >= n)，跳出循环

    ; --- sum += array[i] ---
    MOV SI, CX          ; 准备计算地址偏移
    ADD SI, SI          ; SI = i * 2 (处理字节对齐)
    ADD AX, [BX+SI]     ; AX = AX + array[i]

    INC CX              ; i++
    JMP SUM_LOOP        ; 无条件跳回循环头部

SUM_END:
    MOV SUM, AX         ; 循环结束，把累加好的总和写入全局变量 sum 中
```

---

**祝你逢考必过，超常发挥！**