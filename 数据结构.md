# 1.小测的收获

## 零、 本地终端调试问题

### 1. 终端卡死/乱码假象

- **误区**：在终端输入代码或按回车后，屏幕被各种系统进程信息刷屏（不小心运行了 `top` 命令）。
    
- **解决**：用 `Ctrl + C` 强制中断进程。
    
- **误区**：程序没有任何反应，回车只能换行。
    
- **真相**：程序死循环了（例如 `while` 循环忘记写步长更新 `j++`），或者正在无休止地等待输入。按 `Ctrl + C` 强制中断进程。
    

### 2. 多行输入

- **手动结束输入**：当使用 `while(cin >> x)` 读取不知数量的多行数据时，程序不会因为你按了回车就停止。必须在新的一行按下 **`Ctrl + D`**（Mac）或 **`Ctrl + Z`**（Windows）发送 EOF（文件结束符）信号。

- **输入完了输出在哪里**：vscode中，输入完了之后输出会自动跳出来，并且往往没换行。

---

## 一、 输入流（I/O）与缓冲区

这是新手期最容易翻车的模块，核心在于理解 `cin` 和缓冲区的工作机制。

### 1. `cin >>` 类型的贪婪与残留

- **工作原理**：**自动跳过前导空格/换行**，读取连续字符，**遇到空格、Tab、换行符立即停止**
    
- **致命缺陷**：它会把导致它停止的那个空白字符（如换行符 `\n`）**留在输入缓冲区里**。
    
- **误区重现**：试图用 `cin >> arr` 去读取 `"push 10"`。
    
    - **错因**：`cin` 遇到 `push` 后面的空格就停了，`arr` 里只有 `"push\0"`，数字 `10` 根本没进数组！
        
    - **正确做法**：顺水推舟，分两次读：`cin >> cmd >> val;`。
        

### 2. `getline()` 类型的被动吞噬

- **工作原理**：**不会自动跳过前导空格/换行**，读取包括空格在内的一整行，**直到遇到换行符 `\n` 停止，并吃掉换行符**

- **getline() 基本语法：string类用 getline(cin,str) 而字符数组用 cin.getline(arr,100)

**👉 情况 A：如果用 `string`（强烈推荐）** 使用全局函数 `getline()`，只需要传两个参数。

```c++
string s;
getline(cin, s); // 直接搞定，完美读入一整行
```

**👉 情况 B：如果你用的是字符数组 `char[]`** 不能用上面的写法！必须使用 `cin` 自带的成员函数 `cin.getline()`，并且**必须强制要求你传入最大读取长度**（为了防越界）。

```c++
char arr[100];
// 参数：(数组名, 最大读取字符数)
// 它最多读取 99 个字符，最后 1 个位置自动留给 '\0'
cin.getline(arr, 100); 
```

- **getline不会跳过缓冲区！**：

    ```c++
    int n; string s;
    cin >> n;        // 读走数字，留下 '\n'
    getline(cin, s); // 迎面撞上 '\n'，直接返回空字符串！
    ```
    
- **解决规范**：若要在 `cin >>` 之后 `getline`，必须加一行清空缓冲区的代码：
    
    ```c++
    cin >> n;
    cin.ignore(); // 吞掉残留的换行符
    ```
    
- 🌟 总结复习（千万别忘的“拔剑动作”）

不管是哪种 `getline`，只要你在它**之前**使用了普通的 `cin >>`（比如读了组数 $n$），都请千万记得在中间加一句 **`cin.ignore();`** 去吃掉那个要命的换行符，否则你的 `getline` 就会直接读到一个空串！

### 3. `while (cin >> x)` 应该用在哪里？

- **误区**：在已知循环次数的 `for` 循环内部，又套了一个 `while(cin >> x)`。这会导致内层 `while` 一次性吃光所有测试样例。
    
- **正确情境**：**只在**题目没有给定总组数，要求“读到文件结束 (EOF)”时，才作为最外层循环使用。

### 4. string类的输入or字符数组的输入：都是cin >> 类型

- 都是跳过缓冲区，读取一连串的字符，遇到空格 or 换行符停止读取，遵循cin >> 的语法

- string类非常好用！例如下面这一段代码，如果用字符数组则要一个一个字符比对
```c++
string op;
cin >> op;//string的输入是直接到空格or换行结束，也就是截取一连串的字符
if (op == "push"){
	int x;
	cin >> x;
	sta.push(x);
}
```

### 5. 总而言之

- ==不要读入空格：用 cin >> 类型语法，推荐使用string类而不是字符数组
- ==需要读入空格：用 getline() 类型语法，注意string类和字符数组语法不同

---

## 二、 数组越界与死循环：逻辑杀手

### 1. 死循环：被遗忘的 `j++`

- **错例**：
    
    ```c++
    int j = 0;
    while(arr[j] != '\0') {
        // ... 逻辑判断 ...
        // 忘记写 j++; 导致永远在检查 arr[0]，程序卡死
    }
    ```
    
- **最佳实践**：遍历数组首选 `for` 循环 `for (int j = 0; arr[j] != '\0'; j++)`，把步长更新锁死在循环头上，防呆设计。

- ==在vscode里面，不会提示死循环！所以你的输入始终没有回报！==

### 2. 数组越界（Segmentation Fault）

- **错例 （找最小值逻辑错位）**：
    
    ```c++
    int tem = top_index;
    while(tem != -1) {
        tem--; // 如果此时 tem 是 0，自减后变成 -1
        if(data[tem] < min) // 越界访问 data[-1]，程序崩溃！
    }
    ```
    

---

## 三、 C风格字符串 vs C++ `std::string`

### 1. 字符数组 `char[]` (C 风格)

- **本质**：连续的内存块，自动以**隐藏字符 `'\0'`** 作为结束标志。
    
- **缺点**：长度不可变，容易越界；不能直接用 `==` 判断相等（必须用 `<cstring>` 库里的 `strcmp(a, b) == 0`）；不能用 `+` 拼接。
    

### 2. `std::string` (现代 C++ 利器)

- **包含库**：`#include <string>`
    
- **绝对优势**：自动扩容；直接用 `==` 判断相等；直接用 `+` 拼接；自带获取长度函数。
    
- **范围 for 循环（极其优雅的遍历）**：
    
    ```c++
    for(char ch : s) {
        // 自动从头到尾取出每一个字符，绝不会越界
    }
    ```
表示从s里面从头开始，遍历拿出每一个ch。

其中，ch : s可以理解为“ch in s”

---

## 四、 用string类解决问题的典例：非常好用！

- 如果用字符数组：需要一个一个字符比对！![[file-20260409190300699.png]]
- 如果用string类：不用一个一个字符比对！![[file-20260409190345476.png]]

# 2.string类入门学习

在使用这些魔法之前，请永远确保代码开头有：`#include <string>`

## 一、 输入：如何把文字装进 `string`？

- **读取连续字母（遇到空格就停）**：
    
    这是刷题中最常见的输入方式，适用于绝大多数单次或不含空格的输入。
    
    ```c++
    string s;
    cin >> s; 
    ```
    
- **读取一整行（包含空格）**：
    
    适用于句子、包含空格的特殊格式要求。
    
    ```c++
    string s;
    getline(cin, s); 
    // ⚠️ 避坑提醒：如果在它之前用了 cin >> 读数字，记得中间加一句 cin.ignore();
    ```
    

## 二、 基础函数：长度与判空

- **获取长度**：
    
    `size()` 和 `length()` 是一模一样的，挑一个顺手的用（建议 `size()` 因为写着短）。
    
    ```c++
    string s = "hello";
    int len = s.size(); // 结果是 5
    ```
    
- **判断是否为空**：
    
    不要写 `if(s.size() == 0)`，直接用 `empty()`。
    
    ```c++
    if (s.empty()) {
        // 字符串为空时执行
    }
    ```
    

## 三、 遍历：如何逐个处理字符？

- **🌟 范围 for 循环（防呆设计，无脑首选）**：
    
    不用管下标，不用担心越界，它会自动从头走到尾。

    
    ```c++
    string s = "hello";
    for (char ch : s) {
        // 每次循环拿出来的 ch 依次是 'h', 'e', 'l', 'l', 'o'
    }
    
    // 💡 进阶技巧：如果你想在遍历时**修改**字符，加上引用符号 &
    for (char &ch : s) {
        if (ch == 'h') ch = 'H'; // s 变成了 "Hello"
    }
    ```
    
- **传统下标遍历（需要用到位置信息时用）**：
    
    当你不仅需要知道字符是什么，还需要知道它在**第几个位置**时使用。

    
    ```c++
    for (int i = 0; i < s.size(); i++) {
        // 通过 s[i] 访问或修改当前字符
    }
    ```
    

## 四、 增删改：字符串的魔术操作

- **无脑拼接**：
    
    直接用加号 `+` 或 `+=`，`string` 会自动帮你搞定内存扩容。
    
    ```c++
    string ans = "Leet";
    ans += "Code"; // 变成 "LeetCode"
    ans += '!';    // 也可以直接加单个字符
    ```
    
- **把 `string` 当栈来用（高频神技）**：
    
    刷消消乐、括号匹配、单调栈等题目时，直接用 `string` 当栈，最后还能直接 `cout` 输出。
    
    ```c++
    string st = "";
    st.push_back('a'); // 压栈：在末尾加字符 -> "a"
    st.push_back('b'); // -> "ab"
    
    char top_ch = st.back(); // 获取栈顶元素 -> 'b'
    st.pop_back();           // 弹栈：删除末尾字符 -> 变回 "a"
    ```
    

## 五、 高阶玩法：截取与查找

- **截取子串 `substr`**：
    
    提取字符串中间的一小部分。
    
    ```c++
    string s = "HelloWorld";
    // 语法：s.substr(起始下标, 截取长度)
    string sub1 = s.substr(0, 5); // 提取 "Hello"
    
    // 隐藏用法：如果省略第二个参数，就一直截取到最后！
    string sub2 = s.substr(5); // 提取 "World"
    ```
    
- **查找子串/字符 `find`**：
    
    寻找某个字符或单词是不是在字符串里。
    
    ```c++
    string s = "HelloWorld";
    int pos = s.find("World"); // 返回首字母 'W' 的下标：5
    
    // 如果找不到，它会返回一个特殊的值：string::npos
    if (s.find("apple") == string::npos) {
        // 说明没找到
    }
    ```
    

## 配合 `<algorithm>` 的操作

在刷题时，反转和排序字符串是家常便饭。引入 `#include <algorithm>` 库后，你可以直接对 `string` 放大招：


```c++
#include <string>
#include <algorithm>

string s = "edcba";

// 1. 一键反转字符串
reverse(s.begin(), s.end()); // s 变成了 "abcde"

// 2. 一键排序字符串 (按字母表升序)
sort(s.begin(), s.end()); 
```

# 3.二叉树的递归方法

==核心方法：5种基本的二叉树结构，如果被其他结构包含了就打个✅==
![[file-20260414162848352.png]]
==将0的部分作为递归的出口，其他的递归下去就可以！==

# 4.二叉树的输入方法

核心思想：**数组的下标（位置）就是节点的编号**。在输入的时候只要用几个足够大的数组去存（一般从1开始，不存a[0]），之后查数组就像查字典，拿当前编号去数组里面查对应的值或左右孩子。

## 1. 无节点值（仅结构）

**适用场景**：只关心树的形状，不关心节点里装了什么（例如：求树的高度、最大平衡子树）。 **输入格式**：第 i 行输入节点 i 的左孩子和右孩子编号（`0` 表示空节点）。

```cpp
#include <iostream>
using namespace std;

const int MAXN = 10005;
int left_child[MAXN];  // 存放左孩子编号
int right_child[MAXN]; // 存放右孩子编号

int main() {
    int n;
    if (cin >> n) {
        // 依次读入编号 1 到 n 的左右孩子
        for (int i = 1; i <= n; ++i) {
            cin >> left_child[i] >> right_child[i];
        }
        
        // 树建好了，可以从根节点 1 开始遍历了
    }
    return 0;
}
```

## 2. 带有节点的值（结构 + 数据）

**适用场景**：节点里存了具体的数据（例如：二叉树展开为链表、填充右侧指针）。 **输入格式**：第 i 行输入节点 i 的值、左孩子编号、右孩子编号。

```cpp
#include <iostream>
using namespace std;

const int MAXN = 100005;
int val_node[MAXN];    // 存放节点里的值 (货物)
int left_child[MAXN];  // 存放左孩子编号
int right_child[MAXN]; // 存放右孩子编号

int main() {
    int n;
    if (cin >> n) {
        // 依次读入编号 1 到 n 的：值、左孩子、右孩子
        for (int i = 1; i <= n; ++i) {
            cin >> val_node[i] >> left_child[i] >> right_child[i];
        }
        
        // 树建好了，可以从根节点 1 开始遍历了
    }
    return 0;
}
```

## 运用示例（如遍历）

一旦用上面的模板存好了树，在递归遍历时，使用方法非常固定：

```cpp
void traverse(int u) {//习惯上用u表示当前节点，用v表示下一个节点
    if (u == 0) return; // 遇到空节点，直接回头
    
    // 1. 想知道当前节点的值？直接查表获取
    int current_val = val_node[u];
    
    // 2. 想去左孩子？直接查表获取左孩子编号传进去
    dfs(left_child[u]);
    
    // 3. 想去右孩子？直接查表获取右孩子编号传进去
    dfs(right_child[u]);
}
```

## 以层次序输入（一般是完全二叉树）

其常常会使用==数组==来存储，规律为：
- 略过arr[0]，以==层次序==存入数组，编号从1开始，和下标保持一致
- 如果是编号为k，则左孩子为2k，右孩子为2k+1，父节点为k/2

所以按照层次序输入的时候，只需要用一个数组接住即可，然后用倍数关系找父子

# 5.指针类型的理解

在 C++ 中，理解复杂变量声明的最佳方式，就是把星号 `*` 紧贴着类型名看，将其作为一个完整的==“指针类型”==。这种思维模型能极大地减轻阅读复杂代码时的认知负担。

## 1. 核心思维模型：将 `*` 视为类型的一部分

不要把 `*` 看作变量修饰符，而是看作基础类型的延伸。
* **`BinaryNode`**：普通的节点类型。
* **`BinaryNode*`**：一种全新的类型——“节点指针”类型。

这种理解方式在处理引用操作符 `&` 时特别好用。

---

## 2. 实例一：`BinaryNode* &t` （指针的引用）

用“指针类型”的思维方式来拆解这个常见的树结构传参：

* **基础类型看作整体**：`BinaryNode*` （节点指针类型）
* **修饰符**：`&` （引用，即“本尊的别名”）
* **变量名**：`t`

**理解结果**：变量 `t` 是一个引用，它引用的目标类型是 `BinaryNode*`。
**白话解释**：`t` 是某个【节点指针】的替身/别名。你在函数里对 `t` 赋值，就是在直接修改外面那个指针本尊的指向（这正是二叉查找树能直接把新节点挂载到父节点上的原因）。

---

## 3. 实例二：`BinaryNode &*t` （指向引用的指针 —— 语法陷阱）

你提到想把 `BinaryNode &*t` 理解为“单纯是一个引用，引用的是 t 这个指针”。这个推理逻辑非常清晰，但**在 C++ 的实际语法中，这种写法是不合法的（会直接引发编译错误）**。

我们同样用你的思维方式来拆解一下，看看为什么编译器会拒绝它：

* **基础类型看作整体**：`BinaryNode&` （节点对象的引用类型）
* **修饰符**：`*` （指针）
* **变量名**：`t`

**理解结果**：变量 `t` 是一个指针，它指向的类型是 `BinaryNode&`（一个引用）。即**“指向引用的指针”**。

**为什么 C++ 会报错？**
在 C++ 的底层逻辑里，引用（`&`）只是一个别名，它本身并不像变量那样在内存中拥有独立的物理地址。既然没有真实的内存地址，自然就不可能存在一个指针（`*`）去指向它。所以，C++ 明确禁止了“指向引用的指针”这种语法。

> **总结梳理**
> * 想表达**“指向指针的指针”**：写成 `BinaryNode** t` （合法，两层指针）
> * 想表达**“指针的引用”**：写成 `BinaryNode* &t` （合法，且最常用，代表指针本尊）
> * 想表达**“指向引用的指针”**：写成 `BinaryNode &*t` （**非法**，编译报错）

只要坚持把 `*` 和前面的标识符绑在一起当作“基础类型”，再结合 C++ 从右向左读修饰符的习惯，所有复杂的指针和引用组合都能迎刃而解。

## 应用：二叉查找树的插入操作
```C++
#include <iostream>
using namespace std;

// 模拟书上的 SET 结构（通常用来把 key 和附属数据绑在一起）
template <class KEY, class OTHER>
struct SET {
    KEY key;
    OTHER other;
};

// 二叉查找树的节点定义
template <class KEY, class OTHER>
struct BinaryNode {
    SET<KEY, OTHER> data;
    BinaryNode *left;
    BinaryNode *right;

    // 构造函数，对应书里的 new BinaryNode(x, NULL, NULL)
    BinaryNode(const SET<KEY, OTHER> &x, BinaryNode *lt = NULL, BinaryNode *rt = NULL) {
        data = x;
        left = lt;
        right = rt;
    }
};

// 二叉查找树的完整类实现
template <class KEY, class OTHER>
class BinarySearchTree {
private:
    BinaryNode<KEY, OTHER> *root;
    
    // 书上的核心私有函数：真正干活的递归插入
    // 注意这里的 BinaryNode *&t 就是我们讨论的“指针的引用”
    void insert(const SET<KEY, OTHER> &x, BinaryNode<KEY, OTHER> *&t) {
        if (t == NULL) {
            t = new BinaryNode<KEY, OTHER>(x, NULL, NULL);
        }
        else if (x.key < t->data.key) {
            insert(x, t->left);
        } 
        else if (t->data.key < x.key) {
            insert(x, t->right);
        }
    }
    //如果此处不用指针的引用，则系统会采用值传递方式，即复制一个新的临时指针出来，那后面只是让这个临时指针指向了新节点，新节点并没有连接到原来的树上去。随着临时指针消失，新节点就再也找不到了

    // 写个简单的中序遍历，用来验证插入对不对（左-根-右）
    void printTree(BinaryNode<KEY, OTHER> *t) const {
        if (t != NULL) {
            printTree(t->left);
            cout << t->data.key << " ";
            printTree(t->right);
        }
    }

public:
    // 构造函数，一开始树是空的
    BinarySearchTree() {
        root = NULL;
    }

    // 书上的公开接口：外面调用时只需要传数据，不需要管指针
    void insert(const SET<KEY, OTHER> &x) {
        insert(x, root);
    }

    // 打印树的对外接口
    void print() const {
        printTree(root);
        cout << endl;
    }
};
```
## 复习：三种传递方式

- **值传递（Pass by Value）**：把值复制一遍到新的变量。此时只动用这个复制的变量，不改变原变量
    
- **指针传递（Pass by Pointer）**：传入地址
    
- **引用传递（Pass by Reference）**：给原变量起了个别名。此时改变别名，就是改变原变量

```C++
#include <iostream>
using namespace std;

// 1. 值传递：复制了一份临时变量，无用功
void swapValue(int a, int b) {
    int temp = a;
    a = b;
    b = temp;
}

// 2. 指针传递：传进来的是地址，需要用 * 解引用来修改原件
void swapPointer(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

// 3. 引用传递：传进来的直接是本尊的别名，代码最干净
void swapReference(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int x = 10, y = 20;

    // swapValue(x, y);       // 执行后 x=10, y=20 (没换成)
    // swapPointer(&x, &y);   // 执行后 x=20, y=10 (换成了，注意传参要取地址)
    // swapReference(x, y);   // 执行后 x=20, y=10 (换成了，传参方便)

    return 0;
}
```

# 6.建议使用指针的引用！

要不要加 `&`，完全取决于这趟递归的**目的**。我们可以把递归场景分为两类：

## 1. 场景一：需要“修改树”
**👉 必须使用指针的引用 `Node* &`**

如果任务是**破坏或建立原有的连接关系**，例如：
* 插入新节点（找到空位并挂载）
* 删除旧节点（移除节点并把子树拼接到别处）

**原因**：你需要直接操作父节点内部的那个指针本尊。如果不加 `&`，你修改的只是复制出来的一个临时指针，真实的树结构根本没变，代码逻辑就会直接失效。

## 2. 场景二：只需要“读取树”
**👉 使用普通指针 `Node*` 即可，且更安全**

如果任务只是**顺着指针寻址，读取沿途的数据**，例如：
* 查找某个值是否存在
* 计算树的高度或节点个数
* 先序 / 中序 / 后序遍历打印

**原因**：
* **不需要改形状**：你并不打算改变原来树的连接关系。
* **性能没影响**：复制一个指针变量仅仅消耗几个字节（32位系统4字节，64位系统8字节），开销极小，完全不用担心超时。
* **代码更安全**：手里拿的是“复印件”，就算在递归深处写错代码把指针指飞了，也不会意外扯断真正的树枝，起到了一种保护作用。

---

## 总结

写递归传参数前，先判断这趟操作的性质：

* **搞基建** ➡️ 拿本尊的钥匙，用 `Node* &`
* **做调研** ➡️ 拿复印件的地图，用 `Node*`

# 7.树的高度的获取方式

在计算机科学中，树的高度（Height）是决定查找、插入等操作时间复杂度的核心指标。获取或控制树高通常有两种主流思路：

1. **需要用时主动获取**：不保存高度，需要时通过遍历整棵树动态计算。
    
2. **初始化后不断维护**：在节点或全局增设属性，在结构发生变更（插入/合并/删除）时，以 $O(1)$ 的开销动态更新。
    
## 第一部分：主动获取

对于一棵普通的二叉树，如果不通过额外空间记录高度，最经典的方法就是利用后序遍历（左、右、根）的递归（保证了自底向上）算法。

### 1. 核心递推数学思想

一棵二叉树的高度，等于它左子树的高度和右子树高度的**最大值加 1**（加 1 是因为要算上当前根节点本身）。

### 2. C++ 代码实现

```C++
#include <algorithm>
using namespace std;

// 二叉树节点结构定义
struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

int getBinaryTreeHeight(TreeNode* root) {
    // 递归终点：到达最底部，此时树高从0开始记录
    if (root == nullptr) {
        return 0;
    }
    
    // 1. 递归计算左子树高度
    int leftHeight = getBinaryTreeHeight(root->left);
    
    // 2. 递归计算右子树高度
    int rightHeight = getBinaryTreeHeight(root->right);
    
    // 3. 核心递推公式：左右子树最大高度 + 1
    return max(leftHeight, rightHeight) + 1;
}
```

## 第二部分：不断维护

当树的变更非常频繁（如频繁的合并、插入、旋转），如果每次操作都调用上面的 $O(N)$ 遍历算法，程序性能会遭遇毁灭性打击。

因此，许多高效的数据结构采用“初始化 + 局部增量维护”的策略，在每次改变树形时，顺手更新高度。

### 1. 不相交集：按高度合并时

在并查集中，树的形状是任意多叉树。为了防止树退化成链表，引入了 `rank`（秩/高度上界）数组。

- **思想**：初始化时，每个独立元素的高度均为 1。只有在 `Union`（合并）操作时，才需要维护高度。
    
- **维护逻辑**：
    
    - **高度不同**：矮树接到高树下，新根的总体高度**不改变**。
        
    - **高度相同**：任意一棵树接到另一棵下，新根的总体高度**必须加 1**。
        

```C++
// 局部维护代码
void unionElements(int p, int q) {
    int rootP = find(p);
    int rootQ = find(q);
    if (rootP == rootQ) return;

    if (rank[rootP] < rank[rootQ]) {
        parent[rootP] = rootQ; // 矮接高，rootQ高度不变
    } 
    else if (rank[rootP] > rank[rootQ]) {
        parent[rootQ] = rootP; // 矮接高，rootP高度不变
    } 
    else {
        parent[rootQ] = rootP; // 高度相同，强行连接
        rank[rootP] += 1;      // 新根高度顺手 +1
    }
}
```

### 2. AVL 树（二叉平衡树）

AVL 树能够保持严格平衡的秘诀，就在于其**每个节点内部都封装了一个 `height` 属性**。

- **应用场景**：高度平衡控制。AVL 树要求任意节点的左右子树高度差绝对值 $\le 1$。
    
- **随动维护逻辑**：
    
    - **初始化/插入**：新插入的叶子节点高度初始为 `1`。
        
    - **回溯与旋转**：沿着插入路径自底向上回溯，或者在发生“左旋/右旋”改变树形结构时，刷新受影响节点的高度。
        
- **更新公式**：
`node->height = max(getheight(node->left), getheight(node->right)) + 1;`

- ==注意！此处更新公式中，获取子树的高度调用getheight函数并不是在递归
而是直接去获取那个一直在调整的变量height！
但是却不能直接写成 `node->left->height` ==

因为当` node->left==nullptr` 时，即没有左子树时，会出错！
所以最好的方式是写一个getheight函数：
```C++
int getheight(AvlNode *p) const {
    if(p==nullptr) return 0;
    else return p->height;
}
```

## 总结

> [!TIP]
> 
> - **什么时候主动获取（递归）？** -> 树的结构相对静态，或者对单次查询的时间复杂度不敏感。
>     
> - **什么时候动态维护？** -> 频繁执行插入、删除、合并。如AVL树、不相交集

# 8.二叉树 -> 二叉查找树 -> 高级树

### 1. 二叉树（Binary Tree）—— 只是一个“骨架”

二叉树是一个非常纯粹的**结构概念**：每个节点最多有两个儿子（左和右）。它**没有任何关于大小、顺序的限制**。

- 在普通二叉树里，左边放 100，右边放 1，完全合法。
    
- 因为没有顺序，普通二叉树想找一个数，只能用满大街乱转的“穷举遍历”（如前序/中序/后序），时间复杂度是 $O(N)$。
    

### 2. 二叉查找树（BST, Binary Search Tree）—— 注入了“灵魂（顺序）”

BST 是在二叉树的骨架上，加上了**核心规则**：**“左子树所有节点 < 根节点 < 右子树所有节点”**。

- 正因为有了这个大小顺序，我们才能像“二分查找”一样，大的一律往右走，小的一律往左走。
    
- **“先查找后插入”和“删除时找替身（前驱/后继）”，全都是为了“死守这个大小顺序”而发明的经典操作。**
    

### 3. AVL树、B/B+树 —— BST 的“威力加强版”

由于 BST 存在一个致命缺陷：如果按顺序插入 `1, 2, 3, 4, 5`，它会退化成一个线性的链表。为了解决退化问题，科学家们在 **BST 的基础上**引入了不同的“自平衡”机制：

- **AVL树**：依然是二叉的。它在标准 BST 插入/删除后，加上了“高度差不超过 1”的限制（靠旋转修复）。
    
- **B/B+树**：它是 BST 的**多路扩展**。为了在磁盘中少读几次，它把“二叉”变成了“多叉”，但每一层节点内部的键，依然是严格**从左到右有序**的。它的查找、插入、找替身逻辑，本质上就是多路版的 BST。


# 9.高级树的“先操作，后调整”规律

## 0.复习：二叉搜索树（BST）基础操作

### 0.1插入：先查找后插入

先查找后插入，找到nullptr时就可以插入，总是插在叶节点上
### 0.2删除：找替身

- 对于零度or一度节点：容易操作

- 对于二度节点：很难直接删除，所以为了方便，化为删除零度or一度节点，即：
	先找替身==（注意此时选左子树最大节点or右子树最小节点，保证顺序）==，覆盖到要删除的节点上去然后再删除那个替身的零度或一度节点

## 1. AVL 树 —— 先操作后调整

AVL 树是二叉查找树（BST）的进化版。它严格建立在 BST “左小右大”的性质之上。

### 先按照BST操作，后调整：

- **先操作（标准 BST 操作）**：
    
    - **插入**：先查找后插入，从根节点开始，找到nullptr之后插入。
        
    - **删除**：找替身，调整为删除叶节点or一度节点。
        
- **后调整（修复平衡性）**：

    - 插入or删除后都可能引发失衡，此时用旋转来消除失衡 
    
    - 通过递归得到左右子树高度。
        
    - 检查高度差，如果失衡，通过 **LL / RR / LR / RL 旋转**进行调整，恢复平衡。
        

## 2. B 树与 B+ 树 —— BST 的多路高维扩展

B/B+ 树为了适应磁盘存储，打破了“二叉”的限制，演化成了**多叉查找树**。

### 先按照BST操作，后调整：

- **先操作（类比BST操作）**：
    
    - **插入**：先查找后插入。最后肯定是插入到叶节点中。
        
    - **删除**：找替身，删了再说。
        
- **后调整（处理关键字数量溢出，分裂or借、合并）**：
    
    - 插入后，如果发生上溢出，则分裂：
    
	    - 中间的键“向上推”给父节点，再一分为二。
        
    - 删除后，如果发生下溢出，则借or合并：
    
	    - 借：父下来兄弟上去。相当于发生一次旋转，保证了顺序。
	    
	    - 合并：父下到左边，再与右边合并。

## 总结

> [!NOTE] 关键结论
> 
> 1. **二叉树**是骨架，**二叉查找树（BST）**赋予了它“左小右大”的灵魂。
>     
> 2. **AVL树**是 BST 的“精修版”（守着二叉，用旋转控高）。
> 3. **B/B+树**是 BST 的“多路版”（变成多叉，用分裂控高）。
> 4. 它们在**查找、插入、找替身**的骨架代码上，完全是一脉相承的。

# 10.模板 (Template) 指南

> [!tip] 核心心法：模板的本质就是“改名字”
> 
> 模板本身不是可执行代码，而是一张“图纸”。
> 
> 一旦加上了 `template <typename T>`，原来的名字（如 `Node`）就不再是完整的名字了，它退化成了一个**“家族姓氏”**。
> 
> **走到哪，`<T>` 这个后缀就得跟到哪！**（例如必须写全名 `Node<T>* p`，告诉编译器你具体要哪个类型的 Node）。

## 一、Level 1：函数模板 (基础模具)

用于解决不同数据类型执行相同逻辑的问题，避免重复写代码。

```C++
// 1. 声明这是一个模板，定义占位符 T
template <typename T>  

// 2. 把原先写具体类型的地方，全部换成 T
T getBigger(T a, T b) {
    return a > b ? a : b;
}

// 【调用方式】
int x = getBigger<int>(5, 3);          // 编译器悄悄生成 int 版本
double y = getBigger<double>(3.1, 4.2); // 编译器悄悄生成 double 版本
```

## 二、有姓有名

**铁律：** 任何涉及到该节点自身类型的地方，都必须带上 `<T>`，这就是它名字的一部分，和 Node 共同组成了“姓 + 名”的格式。

```C++
template <typename T>
struct Node {
    T data;             // 节点装的数据，类型是 T
    Node<T>* next;      // 指向下一个节点的指针，必须带 <T>
    Node<T>* left;      // 二叉树指针同理
};

// 【调用方式】创建时必须明确类型
Node<int>* myIntNode = new Node<int>();       
Node<char>* myCharNode = new Node<char>();    
```

## 三、复读声明

在面向对象编程中，通常在类内声明，类外实现。一旦加上模板，类外的实现语法需要特别注意作用域和类型声明。

**1. 类内声明（在最上方的template）**

```C++
template <typename T>
class LinkedList {
public:
    Node<T>* head; // 必须带 <T>
    
    LinkedList(); 
    void insert(T value); 
};
```

**2. 类外实现（一定要复读template声明）**

```C++
// 构造函数实现
template <typename T>               // 每次都要先复读声明
LinkedList<T>::LinkedList() {       // 加上 LinkedList<T>:: 作用域
    head = new Node<T>(); 
}

// 成员函数实现
template <typename T>               // 再次复读声明
void LinkedList<T>::insert(T value) { // 返回值类型后，加上作用域
    Node<T>* newNode = new Node<T>();
    newNode->data = value;
    // ... 插入逻辑 ...
}
```

## 四、总结

1. **复读声明：** 函数或类的正上方，加上 `template <typename T>`
    
2. **有姓有名：** 出现时，名字里带上 `<T>`（极易错写成 `Node*` 而不是 `Node<T>*`）

3. *****