# BenchAudit 相关论文阅读收获

## 一、我目前对这个方向的理解

结合 [Workspace-Bench](https://workspace-bench.github.io/)，我现在理解，我们想研究的对象比普通问答数据集复杂得多。Workspace-Bench 要评测智能体能否在真实工作区中寻找相关文件、理解隐含上下文、修改材料，并按照评分规则交付结果。一个任务背后还连着文件依赖、评分细则、运行环境和完整执行过程。任何一环出问题，最终得分都可能偏离智能体的真实能力。

所以我目前对 BenchAudit 的理解是，给定一个已经构建好的 benchmark，尽量少依赖人工，让大语言模型或智能体系统检查任务材料和执行证据，找出会破坏评测有效性的缺陷，并给出可以复核的证据、根因和修复建议。

这里的“问题”比普通数据集里的错标签更复杂，至少包括以下几层。

1. 任务说明层。要求含糊、关键约束缺失、不同段落互相矛盾，或者任务本身无法完成。
2. 标准答案层。参考程序、答案文件或预期结果存在逻辑错误，或者偷偷使用了说明里没有写出的假设。
3. 评分器层。测试覆盖不足、规则过窄、容差不合理、模型裁判有偏差，导致正确答案被拒绝或错误答案通过。
4. 环境层。依赖缺失、路径错误、版本冲突、网络资源失效，导致测到的是环境配置能力，甚至只是运气。
5. 安全与奖励投机层。智能体可以读取答案、修改测试、影响评分器，或者用提示注入骗过模型裁判，从而不完成任务也拿到高分。
6. 执行过程层。很多缺陷在静态文件里看不出来，只有结合运行轨迹、工具报错和最终评分，才能判断失败来自智能体还是 benchmark。

这五篇论文分别覆盖了上面的不同部分。读完以后，我感觉现有方法已经可以替人工完成第一轮筛查，并把最值得检查的问题排到前面，但还无法独立证明一个 benchmark 没有问题。

## 二、BenchGuard

原文见 [BenchGuard](https://arxiv.org/abs/2604.24955)。

### 1. 研究背景和问题定义

执行型智能体 benchmark 已经不再是一张“问题和答案”表。一个任务通常同时包含自然语言说明、参考程序、评分脚本和运行环境。作者认为，许多所谓的智能体失败其实来自这些材料之间的不一致。人工专家容易受自己写出的参考解法影响，把只存在于脑中的默认条件当成任务已经写清楚了。多轮人工检查仍可能漏掉跨文件、跨组件的问题。

BenchGuard 将审计定义为跨材料一致性检查。它重点寻找四类缺陷，分别位于标准答案、评分逻辑、任务说明和运行环境。完整分类进一步拆成 14 个子类。

### 2. 自动化审计流程

BenchGuard 的输入是一个执行型任务的四类核心材料。

1. 自然语言任务说明。
2. 参考程序或标准解法。
3. 评分脚本。
4. 环境配置。

系统先把这些材料整理成统一上下文，再并行运行大模型检查协议和确定性的静态检查。大模型按照六个阶段处理每个任务。

1. 理解任务。提取目标输出、格式和约束。
2. 检查参考程序。核对算法、指标、数据处理和输出格式。
3. 检查评分逻辑。判断评分器是否真正测量任务要求，是否存在偏差、覆盖不足或说明与评分不一致。
4. 检查任务说明。寻找遗漏、矛盾和不可完成条件。
5. 检查环境。寻找依赖、路径和资源约束问题。
6. 合并结果。利用“一个修复能否同时消除多个现象”来合并同根问题，同时把过大的问题拆成原子缺陷。

如果已经有智能体答案或运行轨迹，BenchGuard 会把生成程序、评分结果和日志追加到上下文中，执行动态审计。动态证据能够暴露静态阅读很难确认的问题，例如评分器是否会拒绝顺序不同但语义正确的输出。

最终输出是结构化问题记录。每条记录带有缺陷类别、严重程度、置信度和源文件级证据。系统抑制过低置信度的结果，把较可信的发现交给专家裁决。

### 3. 大语言模型在其中的作用

大语言模型直接承担跨材料语义核对。它既要读懂任务意图，也要理解参考程序和评分代码。静态检查负责容易机械判断的部分，大模型负责那些需要语义和领域常识的问题。多个模型的发现还能组成并集，提高召回率。

这条路线要求模型跳过笼统的“任务好不好”，直接指出哪两份材料在什么地方对不上，并附上可以让人复查的证据。

### 4. 实验设计

论文在两个科学智能体 benchmark 上实验。

| 实验对象 | 任务规模 | 评分方式 | 用作参照的问题 |
| --- | --- | --- | --- |
| ScienceAgentBench | 102 项科学数据分析任务 | 确定性 Python 脚本 | 12 个由原作者确认的问题 |
| BIXBench Verified-50 | 50 项生物信息学任务 | 模型裁判、字符串匹配、数值范围检查 | 17 个专家修订任务，拆成 24 个原子问题 |

审计模型包括 Gemini 3.0 Flash、Gemini 3.1 Pro、GPT-5.4、Claude Opus 4.6 和 Claude Sonnet 4.6。作者分别统计严格匹配召回率、包含部分匹配的召回率，以及问题任务内的精确率。所有发现还要经过领域专家复核。

### 5. 主要结果

BenchGuard 在 ScienceAgentBench 中发现了 12 个后来由原作者确认的问题，其中包括会让任务无解的严重说明错误、指标不一致和拒绝正确答案的评分逻辑。单模型表现存在明显差异。加入智能体程序后，Claude Opus 4.6 的严格召回率由 83.3% 提升到 91.7%，五模型并集能够覆盖全部 12 个已知问题。

在 BIXBench 的 24 个原子问题上，最好单模型 Claude Opus 4.6 的严格匹配召回率为 54.2%，五模型并集达到 83.3%，包含部分匹配时达到 95.8%。一个典型问题是参考程序偷偷删除样本或加入性别协变量，但任务说明没有提到这些处理。模型通过比较说明和参考程序发现了不一致。

五模型完整审计 50 个复杂任务花费不到 15 美元，用时不到 12 分钟。这个结果证明大模型审计在成本上可以进入 benchmark 的日常构建流程。

### 6. 局限与启发

模型会产生幻觉，论文明确保留专家复核。论文报告的精确率很大程度上是与已有专家修订的一致率。自动审计发现的新问题可能真的成立，却会因为专家标注里没有记录而被计作不一致。分类体系也主要来自科学计算任务，迁移到办公、网页操作和操作系统任务时需要扩展。

我觉得这篇对我们最直接的启发，是把 Workspace-Bench 的任务说明、支持文件、预期产物、评分细则和环境配置同时交给审计器，让它逐项核对材料之间是否一致。只看任务说明或只看评分脚本都会漏掉关键矛盾。

## 三、Auto Benchmark Audit

原文见 [Automated Benchmark Auditing for AI Agents and Large Language Models](https://arxiv.org/abs/2605.26079)。论文将系统称为 Auto Benchmark Audit，简称 ABA。

### 1. 研究背景和问题定义

ABA 关注大规模和跨 benchmark 的通用审计。不同 benchmark 的任务保存方式差异很大，有的是一组文件，有的是 JSON 里的补丁，有的只是问题和答案。人工逐个理解这些格式很难扩展。论文把问题归为三条轴线。

1. 任务说明问题，包括关键条件缺失、含糊和误导。
2. 环境问题，包括工具、资源、容器和文件系统与任务假设冲突。
3. 评分问题，包括测试过窄、过宽、目标错位或标准答案错误。

### 2. 自动化审计流程

ABA 分为证据收集和智能体审计两大阶段。

第一阶段由 Evidence Collector Agent 下载仓库和数据集，并把不同 benchmark 的材料解析成统一清单。轻量信息直接写入清单，大文件、参考解法、容器配置、轨迹和测试输出则以文件路径引用。这个中间层很重要，因为后续审计提示词、分类标准和输出格式不需要为每个 benchmark 单独改写。

第二阶段由 Auditor Agent 读取统一清单，并使用文件读取、命令行和检索工具检查任务。它有两种模式。

1. 静态模式只读任务定义、测试、评分配置和环境材料。
2. 轨迹模式再加入智能体执行轨迹和测试输出，用来发现静态材料无法证明的运行时问题。

每条输出包括类别、0 到 2 的严重程度、证据路径、问题解释和修复建议。严重程度 2 表示任务基本无法正确完成，严重程度 1 表示有领域知识时可以绕过，0 表示未发现问题。最后再用上游修复合并请求、论文、博客和人工专家进行外部验证。

### 3. 大语言模型和智能体的作用

ABA 中的模型以工具型智能体运行。它会在仓库中定位文件、读取代码和执行命令。证据收集智能体解决格式差异，审计智能体解决语义判断。轨迹模式还让审计器观察任务在真实运行中怎样失败。

这比 BenchGuard 更接近通用审计平台。BenchGuard 的输入结构和六阶段核对更明确，ABA 则强调从一个 benchmark 名称和链接开始，自动整理证据后再审计。

### 4. 实验设计

作者从近期前沿模型使用的 benchmark 和 NeurIPS 2025 数据集与 Benchmark 论文中收集了 168 个 benchmark，覆盖九个领域、34285 个任务。被审计对象包括科学、多模态、专业知识、工具使用、代码、医疗、数学、检索和安全对齐。实验排除了主观性很强、缺乏可验证答案的任务。主实验使用 Claude Code 和 Opus 4.7，固定工具权限和软件版本。

验证采用分层证据。最强证据是维护者独立提交的修复合并请求或主动弃用 benchmark，其次是 BenchGuard 整理的已知问题，最后是人工抽样复核。

### 5. 主要结果

静态审计在 34285 个任务中将 25.7% 标为存在重大问题，另有 15.1% 只有轻微问题。不同领域差异很大。数学任务的重大问题比例为 13.2%，安全与对齐任务达到 42.2%。作者认为，环境和评分流程越复杂，隐含假设越多，问题越容易发生。

在 Terminal-Bench 2 维护者独立修复的 21 个问题上，轨迹审计严格匹配 14 个，部分匹配计入后覆盖 17 个，对应 66.7% 和 81.0% 的召回率。对 PR 之外人工复核的重大问题，严格精确率为 72.7%，包含部分匹配时为 81.8%。

与 BenchGuard 使用同一批已知问题比较时，ABA 在 ScienceAgentBench 严格找回 10 个问题，在 BIXBench 的严格召回率为 62.5%，高于对应的 BenchGuard 单模型结果 54.2%。

轨迹模式在八个可比较 benchmark 上平均多标出 8.5% 的重大问题任务，总体多标出 6.1% 的问题任务。过滤审计标出的问题以后，五个 benchmark 的模型得分平均提高约 10.5%，排名也会变化。摘要另报告 SWE-bench Verified 和 Terminal-Bench 2 的平均表现分别提高 9.9% 和 9.6%。这说明 benchmark 缺陷会直接改变我们对模型能力的判断。

### 6. 局限与启发

论文最大的难点是缺乏完整真值。维护者修复记录本身并不覆盖所有缺陷，人工复核也带有判断差异。静态和轨迹模式只能部分重合，单一证据源会漏检。论文抽样复核中，静态模式重大问题的严格精确率为 73%，说明仍有明显误报空间。

我从这篇里主要学到两点。第一，应该先把不同 benchmark 的材料整理成统一证据清单，再考虑提示词和模型，这样接入新 benchmark 会容易很多。第二，静态审计和轨迹审计各自能看到不同问题，两种模式都应该保留。

## 四、BenchJack

原文见 [BenchJack](https://arxiv.org/abs/2605.12673)。

### 1. 研究背景和问题定义

BenchJack 研究的是 reward hacking。智能体找到评分器漏洞以后，可以在没有完成真实任务的情况下取得高分。等待模型在正式评测中偶然暴露这类行为既慢又不可靠，事后查看轨迹也只能发现已经发生的攻击。作者因此把审计改成主动红队测试，提前寻找一条可执行的作弊路径。

论文从既有案例中总结出八类缺陷。

1. 智能体和评分器隔离失败。
2. 答案随测试材料一起暴露。
3. 智能体可向评分器注入远程代码。
4. 模型裁判可被提示注入。
5. 字符串匹配过弱。
6. 评分逻辑有空缺。
7. 评分器信任智能体可操控的输出。
8. 智能体权限过大。

### 2. 自动化审计流程

BenchJack 包含三个阶段。

1. 侦察。系统自动搭建 benchmark，定位正式入口、评分函数、任务配置和智能体环境，并画出评分器接触智能体可控数据的位置。
2. 缺陷扫描。智能体依据八类缺陷和 30 项检查清单审查架构与具体任务，生成带位置、解释和严重程度的缺陷账本。系统同时调用 Semgrep 规则、Dockerfile 分析器和基于抽象语法树的信任边界分析器。
3. 攻击构造。系统生成可以通过正式评测入口运行的攻击程序。攻击只能使用正常智能体可观察的信息和可执行的动作，不能提前篡改 benchmark。它反复运行和修改攻击，目标是在不完成任务的情况下把分数推到最高。

论文还加入攻击者与修复者循环。BenchJack 找到攻击以后，另一个代码智能体修补 benchmark，随后 BenchJack 重新攻击。循环一直持续到找不到新攻击，或者剩余问题必须重做 benchmark 架构才能解决。

### 3. 大语言模型和智能体的作用

BenchJack 中的模型不只是“判断有漏洞”。它像红队工程师一样搭建系统、读代码、调用静态分析工具、生成攻击并实际执行。可运行的攻击结果充当缺陷证据，也把风险量化成可被攻击的任务比例。

这一点比单纯输出自然语言批评更强。一个漏洞描述可能是幻觉，一段在官方入口下稳定拿到高分的攻击程序更容易验证。不过，可成功攻击只证明 benchmark 存在风险，不能证明正常模型一定会自发采用这条攻击路径。

### 4. 实验设计和结果

作者审计了十个常用智能体 benchmark，覆盖软件工程、机器学习工程、终端操作、网页导航、桌面操作和网络任务，包括 SWE-bench Verified、SWE-bench Pro、Terminal-Bench、OSWorld、WebArena、MLE-Bench 等。

BenchJack 在全部十个 benchmark 中找到可利用问题，并在九个 benchmark 上让简单攻击取得接近满分的结果。AgentBench 因任务类型较杂，没有达到 90% 的整体攻击比例。系统共报告 219 个缺陷，覆盖八类缺陷。很多单任务问题对总分影响有限，但一个全局隔离漏洞就可能让所有任务同时失效。

在四个不存在致命架构缺陷的 benchmark 上，攻击和修复循环把可攻击任务比例从接近 100% 降到 10% 以下。WebArena 和 OSWorld 在三轮以内降到 0%。实验同时发现，局部补丁常常只能挡住原攻击，重新运行 BenchJack 后又能找到新路径，因此修复后必须重新红队测试。

### 5. 局限与启发

论文没有证明前沿模型会在普通评测中自然生成这些攻击，BenchJack 构造出的利用方式可能比正常模型行为更强。分类体系面向当前智能体 benchmark，遇到新的评分架构还要扩展。完整流程依赖代码智能体能力，大型 benchmark 的搭建和运行成本也较高。红队工具本身还有被用于攻击公开排行榜或评分基础设施的双重用途风险。

我觉得把这篇的方法放到 Workspace-Bench 中，可以单独增加一项评分安全审计，检查智能体能否读取评分细则或答案、修改结果文件以外的评分依赖、控制评分输入、利用模型裁判提示注入，以及它是否拥有超过任务需要的文件和网络权限。

## 五、Can LLMs Detect Benchmark Defects

原文见 [Can LLMs Detect Benchmark Defects?](https://openreview.net/forum?id=QdDcI0Ftvo)。这篇工作关注审计器到底找得准不准。论文当前还是会议评审版本，OpenReview 页面后续可能更新。

### 1. 研究背景和问题定义

自动审计研究经常报告找到了多少候选问题，却缺少独立、完整的真值。作者利用 benchmark 从旧版本到新版本的修改记录，将维护者已经承认并修复的问题作为真值，建立 Task Verification Bench，简称 TVB。

它要求审计器完成两件事。第一是判断旧任务有没有缺陷，第二是说对根因。只猜中“有问题”却指出错误原因，不能指导维护者修复。

### 2. 自动化评测流程

1. 从 SWE-bench 和 Terminal-Bench 的版本更新、人工审计和修复记录中提取缺陷任务与有效任务。
2. 只把旧版本材料交给审计智能体，包括问题说明、测试或测试补丁、标准补丁等，不泄露后续修复。
3. 审计智能体在沙箱中检查材料，输出二元判断和自由文本诊断。
4. 模型裁判将诊断与维护者真值比较，判断“修复审计器指出的问题，能否同时修复真值中的问题”。
5. 分别统计普通判断的精确率和召回率，以及根因匹配后的精确率和召回率。

作者还比较了只验证、先求解再验证、是否访问运行环境等不同流程，用来判断增加执行过程究竟帮助还是干扰审计。

### 3. 实验设计和主要结果

TVB 包含三组数据。

| 数据来源 | 缺陷任务 | 有效任务 | 合计 |
| --- | --- | --- | --- |
| SWE-bench | 129 | 129 | 258 |
| Terminal-Bench 2 | 26 | 26 | 52 |
| Terminal-Bench 1 | 65 | 56 | 121 |

GPT-5.4 在只验证且可使用沙箱的设置下，SWE-bench、TB2 和 TB1 的普通缺陷召回率分别为 62.0%、88.5% 和 90.8%，根因召回率却分别只有 59.7%、34.6% 和 41.5%。在 Terminal-Bench 两组数据上，普通判断比根因判断高出 49 到 54 个百分点。可见“模型觉得哪里可疑”和“模型找到了维护者真正修复的问题”差别很大。

案例分析显示，模型更擅长发现任务说明和测试断言之间的文字层矛盾。抽样的六个真阳性案例都属于这一层。环境问题、Dockerfile 泄漏和标准答案回归等基础设施问题明显更难，抽样的八个 Terminal-Bench 漏检或错因案例都位于这一层。

论文还发现了自我归因锚定。GPT-5.4 先尝试解题，再拿着自己的失败代码做审计时，容易把失败归因给自己，SWE-bench 的根因召回率从 78.3% 降到 53.9%。Gemini 3.1 Pro 在不同流程中的根因召回率保持在约 70% 到 76%，没有出现同样幅度的下降。流程设计和模型选择都可能改变审计结果。

### 4. 局限与启发

版本差异真值仍不完美。基于人工标注的数据可能把一些可接受任务判为缺陷，基于修复合并请求的数据又会漏掉尚未被维护者发现的问题。论文用 77 个案例的人工检查验证模型裁判，同时也确认了这些真值噪声。

这篇论文让我意识到，评价审计系统时不能只统计“报告了多少问题”，还要区分任务级命中和根因级命中。一个真正有用的缺陷报告应该说明具体材料、触发条件、错误影响和最小修复。我们还应避免让同一个智能体带着自己的失败答案审计同一次运行，至少要增加独立审计器或做盲化处理。

## 六、Automated Transcript Analysis

原文见 [Automated Transcript Analysis for Detecting Flaws in Agentic Benchmarks](https://arxiv.org/abs/2607.27518)。

### 1. 研究背景和问题定义

有些 benchmark 问题只有在智能体运行以后才会出现。评分器代码可能看起来正常，但真实轨迹显示工具持续失败、答案已经暴露在环境里，或者任务可以靠猜测通过。大型评测一次会产生大量文本和工具调用记录，纯人工阅读无法扩展。

论文基于 Agentic Benchmark Checklist 选择了四类适合从轨迹中观察的问题。

1. 标准答案或关键答案信息泄漏。
2. 工具失败影响任务完成。
3. 答案空间过小，任务可被猜中。
4. 输出格式不明确，语义正确答案因格式被拒绝。

### 2. 自动化审计流程

作者先制定四套人工评分细则，使用 0 到 3 的严重程度。0 表示无证据，1 表示可能存在，2 表示明确存在但未直接改变通过结果，3 表示会影响通过与否。

随后，人类标注开发集轨迹。由于自然轨迹中的严重缺陷较少，作者人为修改任务环境和提示词，合成带缺陷的开发样本，帮助完善 scanner 提示词。这些合成样本不进入最终结果。

论文使用 Inspect Scout 构建 scanner。每个 scanner 接收轨迹和相应上下文，输出某一类问题的严重程度和理由。提示词在开发集上迭代后冻结，再在任务不重叠的测试集上运行。

测试阶段采用分层后验证。作者按 benchmark 和 scanner 是否报警分层抽样，让人类同时查看 scanner 输出和支持材料，再通过逆概率加权还原总体指标。这种设计能在缺陷稀少时把有限人工时间更多地用于可能的真阳性和假阴性。

### 3. 实验设计

数据来自 Inspect Evals 中的 11 项智能体评测，正文主要列出 SWE-bench Verified、CORE-Bench、MLE-Bench、MLRC-Bench、Terminal-Bench 2、KernelBench、Tau2 和 CVE-Bench 等。用于生成轨迹的模型包括 GPT-5-mini、GPT-5.4、Claude Sonnet 4.6 和 Claude Opus 4.6。scanner 模型为 GPT-5.4 和 Claude Sonnet 4.6。

五名人工评分者标注了 770 条开发集轨迹和 742 条测试集轨迹。论文用人类判断作为参照，报告敏感度、特异度、F1 和二次加权 Cohen's kappa。

### 4. 主要结果

性能随问题类型、benchmark 和 scanner 模型剧烈变化。最好的是 GPT-5.4 的猜测漏洞 scanner，F1 为 0.74，敏感度为 0.93。最差的是 Claude Sonnet 4.6 的工具失败 scanner，F1 只有 0.14，敏感度为 0.17。工具报错很常见，但判断它究竟是预期难度还是环境缺陷，需要 scanner 掌握 benchmark 设计意图，因此最容易误判。

scanner 发现了多类经人工确认的问题。CORE-Bench 的代码或结果文件会直接暴露答案，有些任务的实际答案空间很小，模型不执行完整流程也可能猜中。部分任务采用精确匹配，却没有说明小数位数、颜色名称等格式。SWE-bench Verified 的某些轨迹显示智能体读取上游版本历史并复制人类补丁，绕过了独立解决问题的要求。

组合两个 scanner 可以改变召回和误报的取舍。任意一个报警就算阳性的组合更偏向高召回，要求两个结果一致则更偏向高特异度。这说明多模型分歧本身可以用来安排人工复核优先级。

### 5. 局限与启发

scanner 只能发现轨迹里可见的问题，无法独立检查评分器源代码或完整环境。不同评测提供的上下文不一致，导致泛化困难。人类评分者对一些边界案例也会产生合理分歧，不能把人工标签当成绝对真理。scanner 还容易把一个维度中的多个子问题混在一起，专门化程度需要继续提高。

读完这篇以后，我觉得 Workspace-Bench 需要保存结构化的执行记录。轨迹审计不能只看最终对话文本，还要保存文件读取、文件修改、命令执行、错误信息、评分细项和关键环境状态。缺少这些信息时，很多问题根本不会出现在审计器能够观察到的范围里。

## 七、五篇工作的横向比较

| 工作 | 审计对象 | 输入信息 | 自动化程度 | 模型或智能体的作用 | 能发现的问题 | 人工验证 | 实验对象 | 主要优势 | 主要不足 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BenchGuard | 单任务的组成材料 | 说明、参考程序、评分脚本、环境，可追加程序与轨迹 | 高 | 按固定协议跨材料核对，多模型集成 | 说明、标准答案、评分器、环境不一致 | 必须，专家裁决高置信度发现 | ScienceAgentBench、BIXBench | 结构清晰，证据具体，成本低 | 领域范围较窄，仍有幻觉，精确率受真值不完整影响 |
| ABA | 多种 benchmark 的大量任务 | 仓库、数据集、任务材料、环境、轨迹、测试输出 | 很高 | 收集智能体统一材料，审计智能体调用工具检查 | 说明、环境、评分和标准答案问题 | 必须，用修复记录和人工抽样验证 | 168 个 benchmark、34285 个任务 | 接入范围广，静态与轨迹统一，能研究榜单影响 | 真值稀缺，重大问题仍有误报，运行成本随规模增加 |
| BenchJack | 整个评测架构及信任边界 | 仓库、入口、评分代码、容器、任务配置 | 很高 | 搭建系统、静态扫描、生成并执行攻击、迭代修补 | 奖励投机、答案泄漏、代码注入、权限和隔离问题 | 攻击分数可自动验证，最终仍需安全复核 | 10 个智能体 benchmark | 用可运行攻击证明问题，可量化危害 | 只能证明可利用性，不能证明正常模型会利用，存在双重用途风险 |
| TVB | 审计器本身 | 旧版说明、测试、标准补丁、沙箱 | 评测流程自动化程度高 | 检查任务并给出根因，模型裁判匹配维护者真值 | 以代码 benchmark 的说明、测试、环境和标准答案缺陷为主 | 用 77 个案例核查裁判和真值 | SWE-bench、Terminal-Bench 两代数据 | 首次把根因匹配作为核心指标，能揭示虚高召回 | 版本差异真值有漏标和误标，基础设施问题仍难 |
| Transcript Analysis | 智能体运行轨迹 | 对话、工具调用、运行结果和部分任务上下文 | 高，但依赖人工后验证 | 专用 scanner 对单类问题打分，多模型组合 | 答案泄漏、工具失败、可猜测性、格式歧义 | 必须，分层抽样后验证 | 11 项 Inspect Evals 评测 | 适合大规模动态记录，可发现静态材料看不到的问题 | 只能看到轨迹中出现的现象，跨 benchmark 泛化不稳 |

我把这五篇工作放到同一个流程里以后，感觉它们的关系大致如下。

1. BenchGuard 和 ABA 负责静态材料与常规轨迹中的一致性问题。
2. BenchJack 主动制造对抗行为，检查评分器能否被绕过。
3. Transcript Analysis 对大量真实或红队轨迹做批量扫描。
4. TVB 提供评价审计器的方法，防止系统只会泛泛报警却找错根因。

## 八、我觉得现有工作已经解决了什么

第一，研究已经形成较稳定的审计对象。任务说明、标准答案、评分器、运行环境和轨迹是五类核心材料，单独检查其中一类不够。

第二，跨材料语义核对已经可行。BenchGuard 和 ABA 证明，前沿模型能够发现说明与参考程序、说明与评分器、环境与任务要求之间的矛盾，而且成本可以低到每个任务几美分到几角美元。

第三，审计可以从静态阅读扩展到运行时证据。轨迹会暴露环境漂移、静默失败、答案泄漏和格式拒绝。ABA 和 Transcript Analysis 都显示，动态证据能补充静态审计。

第四，奖励投机可以转化为主动安全测试。BenchJack 生成真实可执行的攻击，用分数验证漏洞，使“评分器可能有风险”变成可复现证据。

第五，审计器本身开始有相对严格的评测。TVB 的根因匹配提醒我们，二元判断远远不够。审计结果必须指向可修复的真实原因。

## 九、我觉得目前还没有解决什么

最明显的问题是缺少完整真值。原作者确认、专家修订、版本差异和维护者修复都只能覆盖一部分问题。新发现无法简单判成误报，人工意见也可能受参考解法影响。

第二个问题是基础设施层缺陷难度很高。模型擅长比较两段文字或一段说明和一个断言，却常常无法沿着容器、依赖、路径、执行入口和评分状态追完整条因果链。

第三个问题是跨 benchmark 泛化不稳定。同一条工具报错在某个 benchmark 中是环境缺陷，在另一个 benchmark 中可能就是题目设计的一部分。审计器需要知道评测设计者真正想测什么。

第四个问题是静态、轨迹和红队三条路线尚未真正合并。现有工作通常偏重其中一条，问题记录的分类、严重程度和证据格式也不统一。

第五个问题是“发现问题”到“安全修复”之间仍有距离。局部补丁可能只封住一条攻击路径，同时引入新的误拒绝。修复后需要重新运行正常评测和对抗审计，检查能力排名是否被意外改变。

## 十、我认为最值得切入的研究问题

结合 Workspace-Bench 的特点，我目前觉得比较合适的切入点，是建立一个面向复杂工作区任务的多证据、可执行、根因级审计流程。第一版可以先保留人工确认，把目标定为减少逐任务检查的工作量。

一个可实施的最小系统可以包含六步。

1. 证据清单。为每个任务统一收集任务说明、支持文件、预期产物、评分细则、环境配置和已知轨迹，并记录文件来源和版本。
2. 确定性检查。先检查缺失文件、坏链接、路径和版本、评分规则引用、不可访问依赖、测试覆盖和格式约束等机械问题。
3. 跨材料审计。让两个相互独立的模型按固定协议检查说明、文件、产物和评分标准是否一致，并要求逐条引用证据。
4. 动态轨迹审计。运行正常智能体和专门设计的探测智能体，扫描工具失败、无效动作、答案泄漏、误判成功和格式歧义。
5. 对抗评分审计。限制在隔离环境中测试文件篡改、越权读取、提示注入、评分输入控制和答案猜测，要求每个高危发现带可复现脚本。
6. 根因合并与人工确认。把多个模型、静态规则和轨迹 scanner 的结果按根因合并。人工只复核高严重度、模型分歧大或会改变得分的项目。

评价这套系统时，至少要分开报告四组指标。

1. 任务级精确率和召回率，用于回答能否找到问题任务。
2. 根因级精确率和召回率，用于回答是否找对真正需要修复的地方。
3. 证据可复现率，用于回答第三方能否用报告中的文件位置或脚本重现问题。
4. 能力影响，用于回答移除或修复问题后，模型得分、排序和能力维度是否发生变化。

我目前最感兴趣的是基础设施层的根因追踪。现有论文对文字层不一致已经覆盖得比较多，但对复杂文件依赖、环境状态和评分流程组成的长因果链处理得还不够好。Workspace-Bench 恰好有大型工作区、隐含文件依赖、异构文件和细粒度评分规则，适合研究“一个失败究竟来自智能体能力、任务材料、环境，还是评分器”。如果能把轨迹里的失败自动追到具体文件依赖或评分规则，并通过干预复现，我感觉会比单独增加一个大模型审计提示词更有研究价值。

## 十一、我这一轮阅读后的总结

我目前的判断是，自动 benchmark 审计已经可以作为人工检查的辅助工具。大模型能以较低成本找出一些专家漏掉的问题，也能通过工具调用和真实攻击提供更强的证据。现阶段比较可靠的用法仍然是让机器先做高召回筛查和证据整理，再由人确认根因并决定怎样修复。

如果后面继续往这个方向做，我觉得不能只看系统报告了多少问题。还要检查它有没有找对根因，证据能不能复现，以及修复后 benchmark 是否更准确地测到了目标能力。能把这几件事做好，BenchAudit 才有可能成为 benchmark 构建和发布前的一项固定检查，生成的也会是有证据、能复核的审计结果。

## 十二、完整论文清单与定位

1. [BenchGuard](https://arxiv.org/abs/2604.24955)
   核心精读。跨任务说明、标准答案、评分器和环境做结构化核对。

2. [Automated Benchmark Auditing for AI Agents and Large Language Models](https://arxiv.org/abs/2605.26079)
   核心精读。通用证据清单、静态审计和轨迹审计，并研究问题任务对榜单的影响。

3. [Auditing the Audit](https://arxiv.org/abs/2607.02586)
   相关方法论。用于反思审计研究自己的真值、抽样、验证和结论风险，适合作为后续评价设计的补充阅读。

4. [Do Androids Dream of Breaking the Game? Systematically Auditing AI Agent Benchmarks with BenchJack](https://arxiv.org/abs/2605.12673)
   核心精读。主动生成奖励投机攻击，并在攻击和修复循环中加固评分器。

5. [Benchmarking the Benchmarks](https://arxiv.org/abs/2607.02577)
   领域审计案例。重点关注工具调用 benchmark 的测量有效性，可以补充“审计结论如何影响能力解释”。

6. [Can LLMs Detect Benchmark Defects?](https://openreview.net/forum?id=QdDcI0Ftvo)
   核心精读。用 benchmark 版本更新构造审计真值，强调根因匹配。

7. [Automated Transcript Analysis for Detecting Flaws in Agentic Benchmarks](https://arxiv.org/abs/2607.27518)
   核心精读。用专用 scanner 批量分析执行轨迹中的有效性问题。

8. [Benchmark Profiling](https://aclanthology.org/2025.emnlp-main.789/)
   相邻方向。它用参数消融和 Ability Impact Score 诊断 benchmark 实际依赖哪些能力，主要解决构念画像，不直接寻找任务错误。它可以在缺陷审计之后回答“修复后的 benchmark 到底在测什么”。

## 参考来源

- [Workspace-Bench 项目页](https://workspace-bench.github.io/)
- [BenchGuard 论文](https://arxiv.org/abs/2604.24955)
- [Auto Benchmark Audit 论文](https://arxiv.org/abs/2605.26079)
- [BenchJack 论文](https://arxiv.org/abs/2605.12673)
- [Task Verification Bench 论文](https://openreview.net/forum?id=QdDcI0Ftvo)
- [Automated Transcript Analysis 论文](https://arxiv.org/abs/2607.27518)
